// Data class to represent a node (city)
// Moved to a separate file for better modularity and future flexibility,
// in case changes to how cities are represented or handled are needed.
data class Node(val name: String)

